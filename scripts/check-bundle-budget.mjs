import { readdirSync,readFileSync } from "node:fs";import { gzipSync } from "node:zlib";import { join } from "node:path";
const dir="apps/web/dist/assets";const files=readdirSync(dir);let total=0;const failures=[];for(const file of files){if(!file.endsWith(".js"))continue;const gzip=gzipSync(readFileSync(join(dir,file))).byteLength;total+=gzip;const limit=file.startsWith("three-")?350_000:file.includes("worker")?20_000:150_000;if(gzip>limit)failures.push(`${file}: ${gzip} > ${limit}`)}if(total>800_000)failures.push(`total JS gzip: ${total} > 800000`);console.log(`bundle gzip total: ${total} bytes`);if(failures.length){console.error(failures.join("\n"));process.exit(1)}
